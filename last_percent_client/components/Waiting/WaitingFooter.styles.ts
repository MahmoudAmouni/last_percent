import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  footer: {
    padding: 30,
    alignItems: 'center',
  },
  batteryText: {
    color: 'rgba(255, 255, 255, 0.3)',
    fontSize: 12,
  },
});
